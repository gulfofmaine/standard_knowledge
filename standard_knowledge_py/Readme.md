# Standard Knowledge (now in Python)

Ok not actually in Python yet, but trying to figure out Maturin, UV, and compilation.

Test with `uv run pytest`

```py
# uv run python

import standard_knowledge
```
